# Credentials — never commit
.env

# Atomic-write temp files (left behind on crash mid-write)
*.tmp

# Python cache
__pycache__/
*.pyc
*.pyo

# Raw scraped CSVs (regenerated daily by the pipeline)
Phones_skroutz/
Laptops_skroutz/
Smartwatches_skroutz/
Tablets_skroutz/

# Cleaned CSVs (regenerated daily by the pipeline)
Clean/
*.csv

# Pipeline logs
logs/
logs4/
*.log

# Old project subfolder with logs
SkroutzProject/

# Unrelated projects / datasets in this directory
sleap_healthPJ/
student_habits_performance.csv
data/
skroutz-data-pipeline

# Old / superseded scripts
TestALLinONE.py
skroutz_SmartwachesFOR.py
Jupiter_PIP_install_File.ipynb

# One-time report generator and its output (not part of the pipeline)
generate_report.py
*.pdf

# Personal watchlist — use watchlist.example.json instead
watchlist.json

# Generated chart images (regenerated daily by charts_from_db.py)
# Exception: the 4 pipeline trend charts are tracked so the README renders on GitHub
charts/*.png
!charts/price_trend_*.png

# Scratch/utility scripts with _ prefix
_*.py

# Daily dated dashboard snapshots (regenerated each run; only dashboard_latest.html is tracked)
dashboard/dashboard_2*.html

# Unrelated files that may appear in this directory
claude_pro_setup_guide.html

# Claude memory
.claude/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
