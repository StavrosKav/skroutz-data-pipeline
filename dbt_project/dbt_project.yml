name: skroutz_tracker
version: "1.0.0"
config-version: 2

profile: skroutz_tracker

model-paths: ["models"]
test-paths:  ["tests"]
seed-paths:  ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  skroutz_tracker:
    staging:
      +materialized: view
      +schema: public
    marts:
      +materialized: view
      +schema: public
